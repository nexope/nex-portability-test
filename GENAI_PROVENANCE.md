# Generative AI provenance

## Initial proof-of-concept generation

- Date: 2026-07-06
- Tool/model: OpenAI Codex, GPT-5-based coding agent
- Human requester: a representative of NEXOPE S.R.L. SOCIETA' BENEFIT
- User prompt summary: create an automated proof-of-concept for a possible NGI Fediversity-oriented NixOS/Node.js portability demonstrator.
- Context supplied to the model: the requested proof had to be separate from other NEXOPE workstreams, funding applications, domain-specific products and pilot data.
- Generated output: the files introduced in the initial `nex-portability-test` directory, including JavaScript implementation, Nix expressions, tests, CI and documentation.

The initial implementation was AI-assisted and was subsequently reviewed, tested and edited before publication. Before any grant submission, a human maintainer must understand and be able to explain every file. Subsequent AI-assisted changes must be logged here or in commit metadata with the model, prompt or interaction summary, affected files and human verification performed.

This public file intentionally contains a concise provenance summary. If this repository is referenced in an NLnet/NGI Fediversity application, the application package must include the fuller prompt provenance log required by NLnet's Generative AI policy.

Purely AI-generated output must not be represented to NLnet as independently authored human work or claimed as a payable project result. This pre-application proof is Background unless a future MoU explicitly defines eligible new work after selection.
