# Security policy

This repository is a proof-of-concept, not a production service.

The reference service binds to loopback only, limits request bodies, validates exported schemas, excludes unknown record fields and stores state with restrictive file permissions where supported.

Do not store credentials, personal data or production secrets in the demonstration state. Security reports should be sent through the official NEXOPE contact channels or through GitHub security reporting once enabled for the public repository.
